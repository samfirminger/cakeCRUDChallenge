{
  "name": "cakecrudchallenge",
  "version": "1.0.0",
  "description": "",
  "main": "server",
  "scripts": {
    "start": "node server",
    "start:dev": "nodemon --watch server",
    "heroku-postbuild": "cd client && npm install --only=dev && npm install && npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.19.0",
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "md5": "^2.3.0",
    "sqlite3": "^5.0.0",
    "styled-components": "^5.2.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.6"
  }
}
